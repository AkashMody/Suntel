each post in posts
  .post-card
    h2=post.title
    h3="by " + post.author
    p=post.content
    form(method="get" action="#")
      input(type="hidden" name="postId" value=post.id)
      if role=="Admin" 
        button.adminBtn(value="review") Review